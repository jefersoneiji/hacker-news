{
  "name": "hacker-news",
  "private": true,
  "scripts": {
    "build": "turbo build --filter=server",
    "build:web": "turbo build --filter=web",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "start": "turbo start --filter=server",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "prettier": "^3.1.1"
  },
  "dependencies": {
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}